# aperçus / builds locaux (ne pas versionner)
apercu-local.html
preview.html
*.zip

# exports bruts d'images (Photoshop) — on ne garde que les .webp finaux
img/_[0-9]*.png
img/Map Sortie*.png

# sources Photoshop : trop lourdes pour le dépôt (13 Mo+), à garder en sauvegarde à part
*.psd
*.psb

# images sources brutes déposées à la racine (générations, exports) : on ne versionne
# que le webp recadré et redimensionné qui en sort, dans img/
/ChatGPT Image*.png
/*_source.png

# dépendances / système
node_modules/
.DS_Store
Thumbs.db
.vscode/

# visuels de marque bruts (ChatGPT/Midjourney) : on ne versionne que les
# exports finaux dans img/ (logo.webp, icon.webp, og-studio.webp)
/ChatGPT Image*.png
