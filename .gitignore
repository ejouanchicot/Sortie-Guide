# ============================================================
#  Ce qui ne part pas sur GitHub Pages
# ============================================================

# aperçus / builds locaux
apercu-local.html
preview.html
*.zip

# sources de travail : Photoshop, générations d'images, exports bruts.
# Elles vivent dans sources/, à côté du dépôt mais hors de lui — trop
# lourdes (13 Mo pour le seul .psd) pour ce qu'elles apportent versionnées.
# Seuls les .webp finaux, dans img/, sont suivis.
sources/
*.psd
*.psb
/ChatGPT Image*.png
/*_source.png
img/_[0-9]*.png
img/Map Sortie*.png

# dépendances / système
node_modules/
.DS_Store
Thumbs.db
.vscode/
